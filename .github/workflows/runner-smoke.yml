name: runner-smoke

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/runner-smoke.yml"

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Prove hosted runner starts
        run: |
          echo "RUNNER_SMOKE_PASS"
          uname -a
