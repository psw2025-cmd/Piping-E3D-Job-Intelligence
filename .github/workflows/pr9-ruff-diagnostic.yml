name: pr9-ruff-diagnostic

on:
  push:
    branches:
      - agent/integrate-employer-registry

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/integrate-employer-registry
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install
        run: python -m pip install --upgrade pip && pip install -e ".[dev]"

      - name: Capture Ruff findings
        shell: bash
        run: |
          set +e
          ruff check src tests --output-format=concise > pr9_ruff_diagnostic.txt 2>&1
          rc=$?
          echo "RUFF_RC=$rc" >> pr9_ruff_diagnostic.txt
          cat pr9_ruff_diagnostic.txt
          rm .github/workflows/pr9-ruff-diagnostic.yml

      - name: Commit diagnostic
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pr9_ruff_diagnostic.txt .github/workflows/pr9-ruff-diagnostic.yml
          git commit -m "WIP: capture PR #9 Ruff diagnostics"
          git push origin HEAD:agent/integrate-employer-registry
